import React from "react";

export const Home = () => (
	<div className="text-center mt-5">
		<h1>React con Flux</h1>
		<p>Ejemplo de implementación de useContext(), actions, store, setStore, etc</p>
	</div>
);
